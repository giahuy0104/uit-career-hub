# Database

Thư mục này sẽ chứa migration, seed và tài liệu mô hình dữ liệu sau khi ERD và state machine được chốt ở bước 2.

Không tạo bảng nghiệp vụ trước khi thống nhất trạng thái của `JobPost` và `Application`.

